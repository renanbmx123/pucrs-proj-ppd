typedef struct {
	CONTA conta;
	int codigo, valor;
} TRANSACAO;

typedef struct {
	int ID;
	double Saldo;
} CONTA;

//
int solicita_codigo();
bool confirma();

//Agencia
bool solicita_abertura(TRANSACAO);
bool solicita_autenticacao(TRANSACAO);
bool solicita_fechamento(TRANSACAO);

//Agencia e Caixa
solicita_deposito(TRANSACAO);
solicita_retirada(TRANSACAO);
solicita_consulta(TRANSACAO);
